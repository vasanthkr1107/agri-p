spring.datasource.url=jdbc:mysql://localhost:3306/demo_db
spring.datasource.username=root
spring.datasource.password=123456

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Python FastAPI ML service (returns only crop name)
crop.ml.base-url=http://127.0.0.1:8001

# Signed session tokens (use a long random secret in production)
app.auth.secret=smart-crop-demo-auth-secret-minimum-32-characters-long

# Default admin (created or upgraded on startup if missing)
app.admin.bootstrap.phone=Vasanth#07
app.admin.bootstrap.name=System Administrator
app.admin.bootstrap.password=Admin@123

# OpenWeatherMap configuration for real-time weather integration
# Replace the placeholder value with your real API key.
weather.api.base-url=https://api.openweathermap.org/data/2.5
weather.api.key=e0041feba2c78be0c3f5be5b2738f78d